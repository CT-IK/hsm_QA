services:
  bot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: HSM_bot
    restart: unless-stopped
    env_file:
      - .env
    environment:
      DATABASE_URL: sqlite:////app/data/question_bot.db
    volumes:
      - ./data:/app/data